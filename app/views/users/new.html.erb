<div class="hero">
  <p class="large brandon">Make up, meet <span>Blend<i>UP</i></span></p>
  <p class="small brandon">The world's most advanced makeup brush</p>
  <p class="logotext brandon">MAGN!TONE</p>
  <p class="london brandon">LONDON</p>
</div>

<div class="form-wrap clearfix <%= "ended" if Rails.application.config.ended %>">
  <% if Rails.application.config.ended %>
    <%= render "users/campaign_ended" %>
  <% else %>
    <%= render "users/campaign_ongoing" %>
  <% end %>
</div>